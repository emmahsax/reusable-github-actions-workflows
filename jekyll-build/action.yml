name: Jekyll Build
description: Build a Jekyll site

inputs:
  target_dir:
    default: _site/
    description: The directory to build the Jekyll site to
    required: false

runs:
  using: composite
  steps:
    - run: JEKYLL_ENV=production bundle exec jekyll build --destination ${{ inputs.target_dir }}
      shell: bash
